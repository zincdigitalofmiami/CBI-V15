# Models Directory

Forecasting artifacts and model definitions.

## Structure
- `baselines/`: Simple statistical benchmarks (Naive, Mean).
- `anofox_artifacts/`: Trained AnoFox model states (.duckdb extensions).

**Rule:** Models are artifacts, not code. Code goes in `Scripts/`.

