# Quant Admin Dashboard

This directory contains the frontend for the Quant Admin interface.
It displays reports generated by the TimeSeriesScientist (TSci) Reporter Agent.

## Data Source
- Reads from `tsci.runs` and `tsci.qa_checks` tables in MotherDuck.
- Displays JSON reports stored in the `report_json` column.
