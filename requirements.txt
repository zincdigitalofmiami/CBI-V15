# CBI-V15 Requirements
# Institutional-Grade ZL Forecasting Platform

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0
google-cloud-bigquery>=3.11.0

# ML & Training (Mac M4)
lightgbm>=4.0.0
pytorch>=2.0.0
scikit-learn>=1.3.0

# Technical Analysis
pandas-ta>=0.3.14b  # Vectorized TA library (130+ indicators) - TradingView/Bloomberg compatible

# Data Validation (CRITICAL - Prevents logic inversions)
pandera>=0.18.0  # DataFrame validation (Pydantic for Pandas) - Catches China sentiment bug

# Data Ingestion Helpers (Saves weeks of parsing work)
pycot-reports>=0.1.0  # CFTC COT parsing (handles Legacy vs. Disaggregated automatically)
wasdeparser>=0.1.0  # USDA WASDE parsing (handles Revisionist History)

# Model Interpretability
shap>=0.44.0  # SHAP values for feature importance and logic validation (slope analysis)

# NLP/Sentiment (FinBERT + Zero-Shot Classification)
transformers>=4.35.0  # FinBERT, Zero-Shot Classification (BART-large-mnli)
torch>=2.0.0  # PyTorch (for transformers)

# Dataform (BigQuery ETL)
# Note: Install via npm: npm install -g @dataform/cli

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0

# Optional: Research Tools (see requirements-research.txt)
# ruptures, alphalens-reloaded, tigramite (for research/validation only)
